#' List of Longitduinal datasets
#'
#' Datasets are names "datX" where "X" is a number. Currently there are 19 individual longitudinal datasets and a "meta_data" file summarizing them
#'
#' @format A list with 19 datasets (prefixed with 'dat') and a summary of the datasets "meta_data. 
#'         See the dataset "meta_data" within the list "datasets" (i.e. datasets$meta_data) for a description 
#'         of each dataset, its source, and its meta data. 
#' \describe{
#'   \item{target}{the outcome variable for each dataset}
#'   \item{cluster_id}{the cluster variable for each dataset} 

"datasets"